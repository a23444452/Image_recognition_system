# YOLO 全端影像辨識系統

基於會議共識開發的完整 YOLO 物件偵測系統，整合訓練、推論與即時串流功能。

## 📚 文檔

- 📖 [**系統架構文檔 (ARCHITECTURE.md)**](./ARCHITECTURE.md) - 完整的系統架構設計、技術棧、資料流程與設計決策
- 🛠️ [**開發指南 (DEVELOPMENT.md)**](./DEVELOPMENT.md) - 開發環境設定、編碼規範、測試指南與除錯技巧
- 🚀 [**部署指南 (DEPLOYMENT.md)**](./DEPLOYMENT.md) - Docker 部署、雲端平台部署、監控與備份策略

## 🎯 專案目標

整合兩個獨立專案的優勢：
- **YOLO_No_Code_Training**: 圖形化訓練介面、資料集管理
- **Camera_Detection**: 即時影像串流偵測、Web 介面

建立一個統一的 Web 系統，提供：
1. ✅ **訓練模組**: Web 介面化的訓練設定與執行 (Phase 1)
2. ✅ **串流偵測**: 即時攝影機偵測與 YOLO 物件辨識 (Phase 2C)
3. ✅ **資料集管理**: 建立、管理、預處理資料集 (Phase 2A)
4. ✅ **模型管理**: 註冊、啟用、比較模型 (Phase 2B)
5. ✅ **即時監控**: WebSocket 訓練進度與串流畫面推送

## 🏗️ 系統架構

```
┌────────────────────────────────────────────────────────────────┐
│                    Frontend (React + Vite)                      │
│  ┌──────┬───────────┬────────────┬──────────┬─────────┬──────┐│
│  │ 首頁 │即時串流頁 │訓練配置頁   │訓練監控頁 │資料集頁 │模型頁││
│  │      │(WebSocket)│(Multi-Step)│(WebSocket)│         │      ││
│  └──────┴───────────┴────────────┴──────────┴─────────┴──────┘│
└────────────────────────┬───────────────────────────────────────┘
                         │ HTTP + WebSocket
┌────────────────────────▼───────────────────────────────────────┐
│               FastAPI Gateway (Port 8000)                       │
│  ┌────────────────────────────────────────────────────────┐   │
│  │ Routers: /training | /datasets | /models | /streaming │   │
│  │          /ws (training & streaming WebSocket)          │   │
│  ├────────────────────────────────────────────────────────┤   │
│  │ Services: TrainingService | DatasetService             │   │
│  │           ModelService | StreamingService              │   │
│  ├────────────────────────────────────────────────────────┤   │
│  │ Engines: YOLOTrainer | YOLOInference                   │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────┬─────────────────────────────────────┘
                          │
            ┌─────────────┼──────────────┐
            ▼             ▼              ▼
     ┌──────────┐   ┌─────────┐   ┌──────────────┐
     │ SQLite   │   │ Redis   │   │ File Storage │
     │ (任務/結果)│   │(任務隊列)│   │ (模型/資料集) │
     └──────────┘   └────┴────┘   └──────────────┘
                         │
                    ┌────▼────┐
                    │RQ Worker│
                    │(訓練執行)│
                    └─────────┘
```

## 🛠️ 技術棧

### Backend
- **Framework**: FastAPI 0.104.1
- **Task Queue**: Python-RQ + Redis
- **AI Engine**: Ultralytics YOLO (v5/v8/v11)
- **Computer Vision**: OpenCV, Pillow-SIMD
- **Database**: SQLite (開發) / PostgreSQL (生產)
- **Protocol**: WebSocket (即時串流)

### Frontend
- **Framework**: React 18 + Vite
- **Styling**: Tailwind CSS
- **State Management**: React Hook Form + Context API
- **Charts**: Recharts
- **Icons**: Lucide React

### Infrastructure
- **CI/CD**: GitHub Actions
- **Containerization**: Docker + Docker Compose
- **Monitoring**: Prometheus + Grafana (規劃中)

## 📋 專案結構

```
Image_recognition_system/
├── backend/                    # 後端程式碼
│   ├── routers/               # API 端點
│   │   ├── training.py        # 訓練任務 API
│   │   ├── datasets.py        # 資料集管理 API
│   │   ├── models.py          # 模型管理 API
│   │   └── streaming.py       # 即時串流 API
│   ├── services/              # 業務邏輯層
│   │   ├── training_service.py
│   │   ├── dataset_service.py
│   │   └── streaming_service.py
│   ├── engines/               # 核心引擎
│   │   ├── yolo_trainer.py    # YOLO 訓練引擎
│   │   └── yolo_inference.py  # YOLO 推論引擎
│   ├── models/                # 資料庫模型
│   ├── schemas/               # Pydantic 資料驗證
│   ├── utils/                 # 工具函數
│   ├── tests/                 # 測試
│   ├── main.py               # FastAPI 主程式
│   └── requirements.txt      # Python 依賴
├── frontend/                  # 前端程式碼
│   ├── src/
│   │   ├── components/       # React 元件
│   │   ├── pages/            # 頁面元件
│   │   ├── hooks/            # 自訂 Hooks
│   │   ├── contexts/         # Context API
│   │   ├── utils/            # 工具函數
│   │   └── App.jsx          # 主應用程式
│   ├── package.json
│   └── vite.config.js
├── config/                    # 配置檔案
├── scripts/                   # 自動化腳本
│   ├── setup.sh              # 一鍵安裝依賴
│   ├── start.sh              # 一鍵啟動所有服務
│   └── stop.sh               # 一鍵停止所有服務
├── docs/                      # 文檔
├── .github/                   # CI/CD 配置
│   └── workflows/
│       └── ci.yml
├── docker-compose.yml         # Docker 編排
├── .gitignore
└── README.md                  # 本文件
```

## 🚀 快速開始

### 系統要求
- Python 3.10+
- Node.js 18+
- Redis 6+
- 8GB+ RAM
- GPU (選用，用於加速訓練)

### 方法一：使用自動化腳本（推薦）⭐

專案提供了自動化腳本，一鍵完成環境設定與服務啟動。

#### 1️⃣ 安裝所有依賴

```bash
# 執行自動化安裝腳本
./scripts/setup.sh
```

**腳本會自動完成：**
- ✅ 檢查 Python 3、Node.js、Redis 是否已安裝
- ✅ 建立後端虛擬環境
- ✅ 安裝後端依賴（requirements.txt）
- ✅ 安裝前端依賴（npm install）
- ✅ 建立環境變數檔案（.env）
- ✅ 建立必要目錄（models, datasets, logs）

**預期輸出：**
```
🚀 初始化 YOLO 全端影像辨識系統...
✅ Python 3 已安裝
✅ Node.js 已安裝
📦 設定後端環境...
✅ 後端依賴安裝完成
📦 設定前端環境...
✅ 前端依賴安裝完成
✅ 專案初始化完成！
```

#### 2️⃣ 啟動所有服務

```bash
# 一鍵啟動所有服務（Redis + 後端 + 前端）
./scripts/start.sh
```

**腳本會自動：**
- ✅ 檢查並啟動 Redis（如未運行）
- ✅ 啟動後端 API（http://localhost:8000）
- ✅ 啟動前端開發伺服器（http://localhost:5173）

**預期輸出：**
```
🚀 啟動 YOLO 全端影像辨識系統...
✅ Redis 運行中
🔧 啟動後端 API...
✅ 後端 API 已啟動 (PID: 12345)
🎨 啟動前端...
✅ 前端已啟動 (PID: 12346)

✅ 所有服務已啟動！

🌐 前端: http://localhost:5173
🔌 後端 API: http://localhost:8000
📚 API 文檔: http://localhost:8000/docs

⏹️  停止服務: ./scripts/stop.sh
   或按 Ctrl+C
```

#### 3️⃣ 停止所有服務

```bash
# 停止所有運行中的服務
./scripts/stop.sh
```

**腳本會停止：**
- 後端 API (uvicorn)
- 前端開發伺服器 (vite)
- RQ Worker (如有運行)

**預期輸出：**
```
🛑 停止所有服務...
✅ 後端已停止
✅ 前端已停止
✅ RQ Worker 已停止
✅ 清理完成
```

#### 4️⃣ 啟動 RQ Worker（訓練任務處理）

如果需要使用訓練功能，需要額外啟動 RQ Worker：

```bash
cd backend
source venv/bin/activate
rq worker training
```

---

### 方法二：手動安裝

如果需要更精細的控制，可以選擇手動安裝。

#### 後端設定

```bash
# 1. 建立虛擬環境
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. 升級 pip
pip install --upgrade pip

# 3. 安裝依賴
pip install -r requirements.txt

# 4. (可選) 安裝效能優化套件
# pillow-simd 提供 4-6x 圖片處理效能提升
# 注意：在某些平台上可能需要編譯工具
pip install pillow-simd  # 如果安裝失敗，標準 Pillow 已足夠使用

# 5. 啟動 Redis (需先安裝 Redis)
# macOS: brew install redis && brew services start redis
# Ubuntu: sudo apt install redis-server && sudo systemctl start redis
redis-server

# 6. 啟動 RQ Worker (新開終端)
cd backend
source venv/bin/activate
rq worker training

# 7. 啟動 FastAPI (新開終端)
cd backend
source venv/bin/activate
uvicorn main:app --reload --reload-exclude 'venv/*' --host 0.0.0.0 --port 8000
# 或直接使用: python main.py
```

#### 疑難排解

**問題 1**: `pillow-simd` 安裝失敗
```bash
# 解決方案：使用標準 Pillow（已在 requirements.txt 中）
# pillow-simd 是效能優化版本，但標準版本功能完全相同
```

**問題 2**: Redis 連線失敗
```bash
# 檢查 Redis 是否運行
redis-cli ping  # 應該返回 PONG

# 如果沒有安裝 Redis：
# macOS: brew install redis
# Ubuntu: sudo apt install redis-server
# Windows: 使用 WSL 或 Docker
```

**問題 3**: PyTorch/CUDA 問題
```bash
# CPU 版本（預設）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# GPU 版本（如果有 NVIDIA GPU）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

### 前端設定

```bash
# 1. 安裝依賴
cd frontend
npm install

# 2. 啟動開發伺服器
npm run dev
```

訪問 http://localhost:5173 查看前端介面。

## ✨ 功能特色

### Phase 1: 訓練系統 ✅
- 🏋️ **多步驟訓練配置**: 基本設定、訓練參數、資料集、進階選項
- 📊 **即時訓練監控**: WebSocket 推送進度、損失/mAP 圖表
- 🔄 **背景任務佇列**: RQ + Redis 處理長時間訓練
- 📝 **任務管理**: 查詢、列表、刪除訓練任務

### Phase 2A: 資料集管理 ✅
- 📁 **資料集建立**: 自動分割 train/val (可調整比例)
- 📊 **統計資訊**: 圖片數量、類別分佈、資料集健康度
- ✅ **驗證功能**: 檢查目錄結構、標註檔格式
- 🔧 **YAML 生成**: 自動產生 YOLO 訓練配置檔
- 🖼️ **樣本預覽**: 隨機顯示資料集圖片
- ⚡ **並行處理**: ProcessPoolExecutor 加速檔案操作

### Phase 2B: 模型管理 ✅
- 📦 **模型註冊**: 記錄訓練指標 (mAP@0.5, mAP@0.5:0.95, Precision, Recall)
- ⭐ **模型啟用**: 單一活躍模型系統
- 📈 **模型比較**: 多模型指標對比表格
- 📊 **統計儀表板**: 版本分佈、模型數量、平均指標
- 🏷️ **版本過濾**: 按 YOLO 版本 (v5/v8/v11) 篩選

### Phase 2C: 即時串流偵測 ✅
- 🎥 **攝影機串流**: 支援多攝影機 (camera_id 選擇)
- 🤖 **YOLO 即時偵測**: 整合 YOLOv5/v8/v11 推論引擎
- ⚙️ **動態配置**: 即時調整信心度、IOU 閾值、灰階模式
- 📺 **WebSocket 串流**: ~30 FPS 畫面推送 (Base64 JPEG)
- 🎯 **偵測結果顯示**: 邊界框、類別、信心度即時呈現
- 📊 **即時統計**: FPS、偵測數量監控
- 🎨 **響應式 UI**: 左側控制面板 + 右側畫面顯示

### 跨功能特色 🌟
- 🔌 **WebSocket 雙向通訊**: 訓練進度 + 串流畫面
- 💾 **SQLite 持久化**: 任務、資料集、模型統一管理
- 🎨 **現代化 UI**: Tailwind CSS + React 18
- 📱 **響應式設計**: 支援各種螢幕尺寸
- 🔐 **CORS 配置**: 開發環境跨域支援
- 📚 **Swagger 文檔**: 自動生成 API 文檔

## 📚 API 文檔

啟動後端後，訪問 http://localhost:8000/docs 查看完整的 Swagger API 文檔。

### 主要 API 端點

#### 訓練模組
- `POST /api/v1/training/start` - 啟動訓練任務
- `GET /api/v1/training/{task_id}` - 查詢訓練狀態
- `GET /api/v1/training/list` - 列出所有訓練任務
- `DELETE /api/v1/training/{task_id}` - 停止並刪除訓練任務
- `WS /ws/training/{task_id}` - 訓練進度即時推送 (0.5秒輪詢)

#### 資料集管理
- `POST /api/v1/datasets` - 建立資料集
- `GET /api/v1/datasets` - 列出所有資料集
- `GET /api/v1/datasets/{id}` - 取得資料集詳情
- `GET /api/v1/datasets/{id}/details` - 取得資料集統計資訊
- `PATCH /api/v1/datasets/{id}` - 更新資料集
- `DELETE /api/v1/datasets/{id}` - 刪除資料集
- `POST /api/v1/datasets/{id}/generate-yaml` - 生成 data.yaml
- `GET /api/v1/datasets/{id}/samples` - 取得樣本圖片

#### 模型管理
- `POST /api/v1/models` - 註冊新模型
- `GET /api/v1/models` - 列出所有模型
- `GET /api/v1/models/{id}` - 取得模型詳情
- `GET /api/v1/models/active` - 取得當前活躍模型
- `GET /api/v1/models/statistics` - 取得模型統計資訊
- `PATCH /api/v1/models/{id}` - 更新模型
- `DELETE /api/v1/models/{id}` - 刪除模型
- `POST /api/v1/models/{id}/activate` - 啟用指定模型
- `POST /api/v1/models/compare` - 比較多個模型

#### 即時串流偵測
- `POST /api/v1/streaming/start` - 啟動串流偵測
- `POST /api/v1/streaming/stop` - 停止串流偵測
- `GET /api/v1/streaming/status` - 取得串流狀態
- `PATCH /api/v1/streaming/config` - 更新偵測配置
- `WS /api/v1/streaming/ws` - 即時畫面串流 (~30 FPS)

## 💡 使用範例

### 完整工作流程

#### 1️⃣ 建立資料集
```bash
# 透過 API 建立資料集
curl -X POST "http://localhost:8000/api/v1/datasets" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "my_dataset",
    "source_folder": "/path/to/images",
    "split_ratio": 0.8
  }'
```

#### 2️⃣ 訓練模型
```bash
# 啟動訓練任務
curl -X POST "http://localhost:8000/api/v1/training/start" \
  -H "Content-Type: application/json" \
  -d '{
    "task_name": "my_training",
    "model_type": "yolov8",
    "model_size": "n",
    "dataset_id": "dataset_id_here",
    "epochs": 100,
    "batch_size": 16,
    "img_size": 640
  }'

# 透過 WebSocket 監控訓練進度
# ws://localhost:8000/ws/training/{task_id}
```

#### 3️⃣ 註冊訓練好的模型
```bash
# 註冊模型
curl -X POST "http://localhost:8000/api/v1/models" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "my_model",
    "version": "1.0.0",
    "model_path": "/path/to/best.pt",
    "yolo_version": "v8",
    "model_size": "n",
    "dataset_id": "dataset_id_here"
  }'

# 啟用模型
curl -X POST "http://localhost:8000/api/v1/models/{model_id}/activate"
```

#### 4️⃣ 開始即時串流偵測
```bash
# 啟動串流
curl -X POST "http://localhost:8000/api/v1/streaming/start" \
  -H "Content-Type: application/json" \
  -d '{
    "camera_id": 0,
    "model_path": "/path/to/best.pt",
    "conf_threshold": 0.25,
    "iou_threshold": 0.45,
    "use_gray": false
  }'

# 連接 WebSocket 接收即時畫面
# ws://localhost:8000/api/v1/streaming/ws

# 動態調整偵測參數
curl -X PATCH "http://localhost:8000/api/v1/streaming/config" \
  -H "Content-Type: application/json" \
  -d '{
    "conf_threshold": 0.5,
    "iou_threshold": 0.5
  }'

# 停止串流
curl -X POST "http://localhost:8000/api/v1/streaming/stop"
```

### 前端使用

1. 訪問 http://localhost:5173
2. 點擊「資料集管理」建立資料集
3. 點擊「開始訓練」配置並啟動訓練
4. 訓練完成後，在「模型管理」註冊並啟用模型
5. 點擊「即時偵測」開始串流偵測

## 🔧 開發指南

### 關鍵設計決策

1. **任務隊列**: 使用 RQ (Python-RQ) 處理長時間訓練任務
2. **即時通訊**: 使用 WebSocket 推送訓練進度（0.5秒節流）
3. **資源隔離**: 訓練時暫停串流（避免 GPU 爭搶）
4. **效能優化**: ProcessPoolExecutor + pillow-simd

### 開發規範

- **Python**: 遵循 PEP8，使用 Type Hints
- **React**: 使用 Functional Components + Hooks
- **Git**: Conventional Commits 格式
- **測試**: 單元測試覆蓋率 > 80%

### 效能優化檢查清單

- [x] 使用 ProcessPoolExecutor 處理資料集
- [x] 安裝 pillow-simd 替代 Pillow
- [x] WebSocket 進度推送節流 (0.5秒輪詢)
- [x] 串流 FPS 控制 (~30 FPS)
- [x] Base64 JPEG 編碼優化 (品質 80)
- [ ] 訓練時暫停串流 (避免 GPU 資源衝突)
- [ ] React.memo 避免不必要的重新渲染

## 🧪 測試

```bash
# 後端測試
cd backend
pytest tests/ -v --cov=.

# 前端測試
cd frontend
npm run test
```

## 📦 部署

### Docker 部署

```bash
# 啟動所有服務
docker-compose up -d

# 查看日誌
docker-compose logs -f

# 停止服務
docker-compose down
```

### 生產環境

參考 `docs/DEPLOYMENT.md` 獲取詳細的部署指南。

## 🤝 貢獻指南

1. Fork 本專案
2. 建立功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交變更 (`git commit -m 'feat: add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 開啟 Pull Request

## 📄 授權

MIT License

## 👥 開發團隊

根據專家會議共識開發：
- 後端總架構師
- FastAPI 專家
- UI/UX 設計師
- 效能優化專家

---

**Built with ❤️ using YOLO + FastAPI + React**
